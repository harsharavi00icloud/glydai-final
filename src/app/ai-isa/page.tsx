import { PillarPage, pillarMetadata } from "@/components/pillar/pillar-route";

export const metadata = pillarMetadata("ai-isa");

export default function Page() {
  return <PillarPage slug="ai-isa" />;
}
