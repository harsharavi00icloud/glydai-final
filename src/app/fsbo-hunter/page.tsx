import { PillarPage, pillarMetadata } from "@/components/pillar/pillar-route";

export const metadata = pillarMetadata("fsbo-hunter");

export default function Page() {
  return <PillarPage slug="fsbo-hunter" />;
}
