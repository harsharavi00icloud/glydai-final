import { PillarPage, pillarMetadata } from "@/components/pillar/pillar-route";

export const metadata = pillarMetadata("lead-qualification");

export default function Page() {
  return <PillarPage slug="lead-qualification" />;
}
